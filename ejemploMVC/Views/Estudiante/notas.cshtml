@model ejemploMVC.Models.calculo
@{
    ViewBag.Title = "notas";
}

<h2>notas</h2>
@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div class="form-group">
        <a href="resta/resta.cshtml">Resta</a>
    </div>
    <div class="form-group">
        @Html.LabelFor(Model => Model.nota1, new { @class = "control-label" })
        @Html.TextBoxFor(Model => Model.nota1, new { @class = "form-control", type = "double" })
    </div>
    <div class="form-group">
        @Html.LabelFor(Model => Model.nota2, new { @class = "control-label" })
        @Html.TextBoxFor(Model => Model.nota2, new { @class = "form-control", type = "double" })
    </div>
    <div class="form-group">
        @Html.LabelFor(Model => Model.nota3, new { @class = "control-label" })
        @Html.TextBoxFor(Model => Model.nota3, new { @class = "form-control", type = "double" })
    </div>

        <div class="form-group">
            <input type="submit" value="Create" class="btn btn-success" />
        </div>
}
<br /> <br />
@if (ViewBag.resultado != null)
{
    <p>Resultado:&nbsp;@ViewBag.resultado</p>
 
}
<br /> <br />

